apiVersion: v1
kind: Service
metadata:
  name: enigma-keycloak-postgresql-svc
  labels:
    app: enigma-keycloak-postgresql
spec:
  type: LoadBalancer
  ports:
    - port: 15432
      targetPort: 5432
      protocol: TCP
      name: http
  selector:
    app: enigma-keycloak-postgresql
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: enigma-keycloak-postgresql-deployment
spec:
  selector:
    matchLabels:
      app: enigma-keycloak-postgresql
  replicas: 1
  template:
    metadata:
      labels:
        app: enigma-keycloak-postgresql
    spec:
      containers:
        - name: enigma-backoffice
          image: postgres:12
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
            - name: POSTGRES_DB
              value: keycloak
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata